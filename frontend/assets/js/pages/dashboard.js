/* ============================================
   Spark ERP — Dashboard Page Script
   Pure layout page — module cards link to future
   pages. No data layer yet.
   ============================================ */

import { initLayout } from "../modules/layout.js";

document.addEventListener("DOMContentLoaded", () => {
  initLayout();
});

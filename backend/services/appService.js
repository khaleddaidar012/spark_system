/**
 * Placeholder service layer (Phase 1).
 * Business logic will be implemented in later phases.
 */
const appService = {
  getAppInfo() {
    return {
      name: 'Spark Engineering ERP System',
      version: '1.0.0',
      phase: '1',
    };
  },
};

module.exports = appService;
